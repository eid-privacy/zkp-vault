---
type: tag
---

# Frameworks

## Description
Tags entries that are full ZKP frameworks — integrated toolchains combining a DSL, IR, prover backend, and often a verifier — as opposed to standalone libraries.

## Entries tagged

```dataview
LIST
FROM "zkp-vault"
WHERE contains(tags, "frameworks")
SORT file.name ASC
```
