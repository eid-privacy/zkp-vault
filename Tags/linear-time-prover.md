---
type: tag
---

# Linear-Time Prover

## Description
Tags proof systems where the prover runs in linear time (O(n)) relative to witness/circuit size — a key efficiency property for large-scale applications.

## Entries tagged

```dataview
LIST
FROM "zkp-vault"
WHERE contains(tags, "linear-time-prover")
SORT file.name ASC
```
