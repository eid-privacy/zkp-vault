---
type: tag
---

[Home](../README.md) > [Tags](README.md) > llvm

# LLVM

## Description
Tags entries where LLVM infrastructure is used — e.g., compilers that emit LLVM IR as an intermediate step before producing ZK circuits.

## Entries tagged

```dataview
LIST
FROM "zkp-vault"
WHERE contains(tags, "llvm")
SORT file.name ASC
```
